spring:
  application:
    name: transcoding-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8400

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/